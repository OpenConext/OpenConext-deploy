---
- name: Include docker.yml tasks when Profile runs in a docker container
  include_tasks: "docker.yml"
  when: profile_docker | bool

- name: Include host.yml when Profile runs on a VM 
  include_tasks: "host.yml" 
  when: profile_docker|bool == false
