#---------------------------------------------------------------------
# Example configuration for a possible web application.  See the
# full configuration options online.
#
#   http://haproxy.1wt.eu/download/1.4/doc/configuration.txt
#
#---------------------------------------------------------------------

#---------------------------------------------------------------------
# Global settings
#---------------------------------------------------------------------
global
    # to have these messages end up in /var/log/haproxy.log you will
    # need to:
    #
    # 1) configure syslog to accept network log events.  This is done
    #    by adding the '-r' option to the SYSLOGD_OPTIONS in
    #    /etc/sysconfig/syslog
    #
    # 2) configure local2 events to go to the /var/log/haproxy.log
    #   file. A line like the following can be added to
    #   /etc/sysconfig/syslog
    #
    #    local2.*                       /var/log/haproxy.log
    #
    log         127.0.0.1 local2

    chroot      /var/lib/haproxy
    pidfile     /var/run/haproxy.pid
    maxconn     4000
    user        haproxy
    group       haproxy
    nbproc      1
    ulimit-n    9000
    daemon

    # turn on stats unix socket
    stats socket /var/lib/haproxy/stats

#---------------------------------------------------------------------
# common defaults that all the 'listen' and 'backend' sections will
# use if not designated in their block
#---------------------------------------------------------------------
defaults
    mode                    http
    log                     global
    option                  httplog
    option                  dontlognull
    option		    http-server-close
    option 		    forwardfor       except 127.0.0.0/8
    option                  redispatch
    retries                 3
    timeout http-request    10s
    timeout queue           1m
    timeout connect         10s
    timeout client          1m
    timeout server          1m
    timeout http-keep-alive 10s
    timeout check           10s
    maxconn                 3000


#---------------------------------------------------------------------
# ENGINE frontend
#---------------------------------------------------------------------
frontend engine 127.0.0.1:401

    capture cookie JSESSIONID len 52

    acl url_all         path_beg        -i /
    use_backend         engine_be       if url_all
    default_backend     engine_be

#---------------------------------------------------------------------
# PROFILE frontend
#---------------------------------------------------------------------
frontend profile 127.0.0.1:402

    capture cookie JSESSIONID len 52

    acl url_all         path_beg        -i /
    use_backend         profile_be       if url_all
    default_backend     profile_be

#---------------------------------------------------------------------
# STATIC frontend
#---------------------------------------------------------------------
frontend static 127.0.0.1:403

    capture cookie JSESSIONID len 52

    acl url_all         path_beg        -i /
    use_backend         static_be       if url_all
    default_backend     static_be

#---------------------------------------------------------------------
# TEAMS frontend
#---------------------------------------------------------------------
frontend teams 127.0.0.1:601

    capture cookie JSESSIONID len 52

    acl url_all         path_beg        -i /
    use_backend         teams_be        if url_all
    default_backend     teams_be


#---------------------------------------------------------------------
# engine.acc.surfconext.nl
#---------------------------------------------------------------------
backend engine_be

    # Healthcheck ombouwen naar controle op valide antwoord
    #option httpchk HEAD /heartbeat.html HTTP/1.0
    option httpchk HEAD / HTTP/1.0
    option forwardfor # This sets X-Forwarded-For

    mode http
    balance roundrobin
    cookie JSESSIONID preserve indirect
    option httpclose

    cookie HTTPSERVERID insert nocache indirect

    server acc41 145.100.191.133:401 cookie acc41 check inter 20000 fall 2 rise 2 maxconn 1024
    server acc42 145.100.191.134:401 cookie acc42 check inter 20000 fall 2 rise 2 maxconn 1024

#---------------------------------------------------------------------
# profile.acc.surfconext.nl
#---------------------------------------------------------------------
backend profile_be

    # Healthcheck ombouwen naar controle op valide antwoord
    #option httpchk HEAD /heartbeat.html HTTP/1.0
    option httpchk HEAD / HTTP/1.0
    option forwardfor # This sets X-Forwarded-For

    mode http
    balance roundrobin
    cookie JSESSIONID preserve indirect
    option httpclose

    cookie HTTPSERVERID insert nocache indirect

    server acc41 145.100.191.133:402 cookie acc41 check inter 20000 fall 2 rise 2 maxconn 1024
    server acc42 145.100.191.134:402 cookie acc42 check inter 20000 fall 2 rise 2 maxconn 1024


#---------------------------------------------------------------------
# static.acc.surfconext.nl
#---------------------------------------------------------------------
backend static_be

    # Healthcheck ombouwen naar controle op valide antwoord
    #option httpchk HEAD /heartbeat.html HTTP/1.0
    option httpchk HEAD /NOTICE.txt HTTP/1.0
    option forwardfor # This sets X-Forwarded-For

    mode http
    balance roundrobin
    cookie JSESSIONID preserve indirect
    option httpclose

    cookie HTTPSERVERID insert nocache indirect

    server acc41 145.100.191.133:403 cookie acc41 check inter 20000 fall 2 rise 2 maxconn 1024
    server acc42 145.100.191.134:403 cookie acc42 check inter 20000 fall 2 rise 2 maxconn 1024


#---------------------------------------------------------------------
# teams.acc.surfconext.nl
#---------------------------------------------------------------------
backend teams_be

    # Healthcheck ombouwen naar controle op valide antwoord
    #option httpchk HEAD /heartbeat.html HTTP/1.0
    option httpchk HEAD / HTTP/1.0
    option forwardfor # This sets X-Forwarded-For

    mode http
    balance roundrobin
    cookie JSESSIONID preserve indirect
    option httpclose

    cookie HTTPSERVERID insert nocache indirect

    server acc61 145.100.191.135:601 cookie acc61 check inter 20000 fall 2 rise 2 maxconn 1024
    server acc62 145.100.191.136:601 cookie acc62 check inter 20000 fall 2 rise 2 maxconn 1024

# Statistics
listen statisticts 145.100.191.130:80

     stats enable
     stats uri /stats

