---
dependencies:
- { role: tomcat_app
  , app_name: oidc
  , app_version: "{{ oidc_version }}"
  , app_snapshot_timestamp: "{{ oidc_snapshot_timestamp }}"
  , app_local_war: "{{ oidc_local_war }}"
  , app_group_url: "{{ oidc_group_url }}"
  , app_artifact_id: "{{ oidc_artifact_id }}"
  , deploy_name: oidc
  , deploy_xml_name: ROOT
  , tomcat_context_path: /
  , tomcat_handler_name: tomcat
  , tags: oidc }
