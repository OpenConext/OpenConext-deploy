---

- name: copy logging config
  template:
    src: "{{ item }}.j2"
    dest: "{{ dashboard_dir }}/{{ item }}"
    owner: dashboard
    group: dashboard
    mode: 0740
  with_items:
    - logback.xml
    - application.yml
  notify: 
    - "restart dashboard"
