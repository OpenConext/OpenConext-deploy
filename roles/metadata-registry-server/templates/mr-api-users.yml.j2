# Valid scopes are mr.api.Scope.values(); =>  ["READ", "WRITE", "PUSH"]
apiUsers:
  {% for user in metadata_registry.apiUsers %}
  - {
      name: "{{ user.name }}",
      password: "{{ user.password }}",
      scopes: [ {{ user.scopes|join(', ') }} ]
    }
  {% endfor %}
