- name: restart statsserver
  community.docker.docker_container:
    name: statsserver
    state: started
    restart: true

- name: restart statsgui
  community.docker.docker_container:
    name: statsgui
    state: started
    restart: true

