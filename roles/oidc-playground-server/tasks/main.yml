---

- name: copy config
  template:
    src: "{{ item }}.j2"
    dest: "{{ oidc_playground_dir }}/{{ item }}"
    owner: oidc-playground
    group: oidc-playground
    mode: 0740
  with_items:
    - logback.xml
    - application.yml
  notify:
    - "restart oidc-playground"
