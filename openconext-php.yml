---
- hosts: openconext-php
  sudo: yes
  gather_facts: no
  vars:
    env_lang: php
  roles:
    - common
    - tls
    - httpd
    - { role: openconext-common, tags: ['eb','sr']}
    - { role: openconext-engineblock, tags: ['eb'] }
    - { role: janus, tags: ['sr'] }
